{
  "name": "hyperpacks-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for HyperPacks Airdrop Auto-Sweep",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network hyperevm"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  }
}
