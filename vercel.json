{
  "buildCommand": "cd hyperpacks/frontend && npm install && npm run build",
  "outputDirectory": "hyperpacks/frontend/dist",
  "installCommand": "npm install --prefix hyperpacks/frontend",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/:path((?!api).*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, no-store, must-revalidate"
        }
      ]
    }
  ]
}