############################################
# PRESALE APP CONFIGURATION
############################################
# Copy this file to .env and configure all required values.
# DO NOT commit .env to version control!

############################################
# *** REQUIRED CONFIGURATION ***
# These values MUST be set for the app to work.
# The app will refuse to start without them.
############################################

# Privy Authentication (REQUIRED)
# Get your app ID from https://dashboard.privy.io/
VITE_PRIVY_APP_ID=cmf0n2ra100qzl20b4gxr8ql0

# Token Configuration (REQUIRED)
VITE_TOKEN_PRICE=0.0008
VITE_TOTAL_SUPPLY=100000000
VITE_PRESALE_ALLOCATION=5000000
VITE_PRESALE_ALLOCATION_PERCENTAGE=5


# Presale Timing (REQUIRED)
VITE_PRESALE_DURATION_DAYS=30

# Purchase Limits (REQUIRED)
VITE_MIN_PURCHASE_USD=10
VITE_MIN_PURCHASE_TOKENS=12500

# Bonuses and Rewards (REQUIRED)
VITE_EARLY_BONUS_PERCENT=20
VITE_EARLY_BONUS_HOURS=48
VITE_STAKING_APY=15

# Branding (REQUIRED)
VITE_TOKEN_NAME=HyperToken
VITE_TOKEN_SYMBOL=HYPE
VITE_PROJECT_NAME=HyperToken

# Network Configuration (REQUIRED)
# Configure these for your target blockchain
VITE_CHAIN_NAME=HyperEVM
VITE_CHAIN_ID=999
VITE_RPC_URL=https://rpc.hyperliquid.xyz/evm
VITE_NATIVE_CURRENCY_NAME=HYPE
VITE_NATIVE_CURRENCY_SYMBOL=HYPE

############################################
# *** OPTIONAL CONFIGURATION ***
# These have reasonable defaults if not set.
############################################

# Presale Timing (Optional)
# Unix timestamp in milliseconds, or "auto" for duration-based calculation
VITE_PRESALE_END_DATE=auto

# Branding (Optional - will use defaults if not set)
VITE_HERO_TITLE=HyperToken Presale
VITE_HERO_SUBTITLE=Join the future of DeFi on HyperEVM - Limited time presale opportunity
VITE_BRAND_EMOJI=🚀

# FAQ Configuration (Optional - will use defaults if not set)
VITE_FAQ_PRESALE_DURATION=30 days
VITE_FAQ_TOKEN_DISTRIBUTION_TIME=48 hours
VITE_FAQ_SUPPORTED_WALLETS=MetaMask, OKX Wallet, Coinbase Wallet, Rainbow, and WalletConnect compatible wallets

# Social Links (Optional - will show as disabled links if not set)
VITE_TWITTER_URL=#
VITE_TELEGRAM_URL=#
VITE_DISCORD_URL=#

# Copyright (Optional - will use defaults if not set)
VITE_COPYRIGHT_YEAR=2024
VITE_COPYRIGHT_TEXT=All rights reserved

############################################
# SECURITY NOTES:
# - Never commit your .env file to version control
# - Keep your Privy App ID secure
# - Progress data is fetched dynamically at runtime (not hardcoded in config)
# - Test thoroughly before deploying to production
############################################